# --- Rede ---
listen_addresses = '*'
port = 5432

# --- Citus (obrigatório) ---
shared_preload_libraries = 'citus'

# --- Conexões ---
max_connections = 200

# --- WAL / replicação lógica (Citus usa) ---
wal_level = logical
max_wal_senders = 16
max_replication_slots = 16
max_worker_processes = 32

# --- Memória (ajuste conforme sua máquina) ---
shared_buffers = 256MB
work_mem = 16MB
maintenance_work_mem = 128MB

# --- Logs ---
logging_collector = on
log_min_messages = warning
log_line_prefix = '%m [%p] %u@%d '

# --- Citus (opcionais úteis) ---
# citus.max_worker_nodes_tracked = 32
# citus.shard_count = 32